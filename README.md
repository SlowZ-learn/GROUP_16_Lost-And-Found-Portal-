# **LOST AND FOUND PROJECT**

A simple Lost And Found Project web application using HTML, CSS, and JavaScript.

### KEY FEATURES

- **NO Backend Required:** The application uses the browser's 'LocalStorage' to temporarily store user data.
- **Prototype Ready:** This allows the browser to function without a 'backend database' making it suitable for testing.

> **Note:** However, data stored in localStorage may be cleared.
